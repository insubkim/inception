version: "3.9"  # optional since v1.27.0

services:
  nginx:
    build: ./requirements/nginx/.
    volumes:
      - wordpress_php:/usr/share/webapps/wordpress
    networks:
      - net
    ports:
      - "443:443"
    depends_on:
      - wordpress
  wordpress:
    build: ./requirements/wordpress/.
    volumes:
      - wordpress_php:/usr/share/webapps/wordpress
    networks:
      - net
    depends_on:
      - mariadb
  mariadb:
    build: ./requirements/mariadb/.
    volumes:
      - wordpress_db:/Users/insub/wordpress_db
    networks:
      - net

volumes:
  wordpress_php:
  wordpress_db:

networks:
  net:
    driver: bridge
